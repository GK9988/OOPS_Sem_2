/*
Destructors:
    1. Opposite of constructors.
    2. They are used to destroy the objects or deallocate memory alloted to objects.
    3. Automatically invoked when an object goes out of scope.
    4. Also invoked when memory is de-allocated using delete().
    5. Same name as of class but preceededc by tilde ~ operator.
    6. Do not have any return type, nor void.
    7. Do not return anything as well as they do not accept any arguement.
    8. Dan't overload destructors.
*/

#include <bits/stdc++.h>
using namespace std;
